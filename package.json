{
  "dependencies": {
    "axios": "^0.24.0",
  "name": "flask_react",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:5001"
  },
    "scripts": {
    "start": "react-scripts start",
    "start-backend": "quenv/bin/flask run --no-debugger",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
